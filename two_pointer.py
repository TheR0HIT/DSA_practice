def sorted(self,arr):
    left,right=0,len(arr)-1
    while left<right:
        arr[left]**2
        arr[right]**2
        if arr[left]<arr[right]:
            return arr[left]
        else:
            return arr[right]